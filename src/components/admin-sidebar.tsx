"use client";

import Link from "next/link";
import { usePathname, useSearchParams } from "next/navigation";
import type { SessionUser } from "@/lib/auth";
import { LogoutButton } from "@/components/logout-button";

type ProjectNavigation = { id: string; name: string };
const projectMenus = [["projects", "Dashboard HP", "fa-chart-line"], ["media", "จัดการรูปภาพโครงการ", "fa-images"], ["house-types", "จัดการแบบบ้าน", "fa-house"], ["facilities", "สิ่งอำนวยความสะดวก", "fa-star"], ["promotions", "จัดการข้อมูลโปรโมชั่น", "fa-tags"], ["news", "จัดการข้อมูลข่าวสาร", "fa-newspaper"], ["leads", "รายชื่อผู้ลงทะเบียน", "fa-user-check"]] as const;

function initials(name: string) { return name.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase() || "AD"; }

export function AdminSidebar({ user, projects }: { user: SessionUser; projects: ProjectNavigation[] }) {
  const pathname = usePathname(); const searchParams = useSearchParams(); const selectedProject = searchParams.get("project");
  const linkClass = (href: string) => `flex items-center gap-3 rounded-xl px-3.5 py-2.5 text-sm font-medium transition ${pathname === href ? "bg-indigo-600 text-white shadow-lg shadow-indigo-950/30" : "text-indigo-200 hover:bg-indigo-900/60 hover:text-white"}`;
  const projectHref = (resource: string, projectId: string) => `/admin/${resource}?project=${projectId}`;
  return <aside className="flex w-full shrink-0 flex-col bg-[#1e1b4b] text-white md:min-h-screen md:w-72"><div className="border-b border-indigo-950/40 bg-[#110e3b] p-5"><Link href="/admin" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-full bg-indigo-500 text-sm font-extrabold shadow-md">{initials(user.name)}</span><span><span className="block text-sm font-semibold">{user.name}</span><span className="mt-0.5 flex items-center gap-1.5 text-[11px] text-indigo-300"><i className="size-2 rounded-full bg-emerald-400" />{user.role === "SUPER_ADMIN" ? "Super Admin" : "ผู้ดูแลระบบ"}</span></span></Link></div><div className="flex-1 overflow-y-auto p-4"><p className="mb-2 px-3 text-[10px] font-bold tracking-[0.16em] text-indigo-400">WORKSPACE</p><nav className="space-y-1"><Link href="/admin" className={linkClass("/admin")}><i className="fa-solid fa-chart-pie w-4 text-base" />Dashboard MD <span className="ml-auto text-[10px] opacity-70">ภาพรวม</span></Link>{user.role === "SUPER_ADMIN" && <Link href="/admin/users" className={linkClass("/admin/users")}><i className="fa-solid fa-users-gear w-4 text-base" />จัดการผู้ใช้งาน</Link>}<details open className="group mt-3"><summary className="flex cursor-pointer list-none items-center justify-between rounded-xl px-3.5 py-2.5 text-sm font-semibold text-indigo-100 hover:bg-indigo-900/60"><span className="flex items-center gap-3"><i className="fa-solid fa-building w-4 text-base text-indigo-300" />Mida Property</span><i className="fa-solid fa-chevron-up text-xs text-indigo-300" /></summary><div className="mt-2 space-y-1"><Link href="/admin/projects" className="flex items-center gap-2 rounded-lg bg-[#5b37ff] px-3 py-2.5 text-xs font-bold text-white shadow-sm transition hover:bg-[#6747ff]"><i className="fa-solid fa-circle-plus" />โครงการใหม่</Link>{projects.map((project) => <details key={project.id} open={selectedProject === project.id} className="group/project"><summary className={`flex cursor-pointer list-none items-center justify-between rounded-lg px-3 py-2.5 text-xs font-semibold transition ${selectedProject === project.id ? "bg-indigo-900/60 text-white" : "text-indigo-200 hover:bg-indigo-900/45 hover:text-white"}`}><span className="flex items-center gap-2 truncate"><i className="fa-solid fa-location-dot text-indigo-400" /><span className="truncate">{project.name}</span></span><i className="fa-solid fa-chevron-down text-[10px] transition group-open/project:rotate-180" /></summary><div className="my-1 ml-3 space-y-0.5 border-l border-indigo-800/70 py-1 pl-3">{projectMenus.map(([resource, label, icon]) => <Link key={resource} href={projectHref(resource, project.id)} className={`flex items-center gap-2 rounded-md px-2 py-1.5 text-[11px] transition ${pathname === `/admin/${resource}` && selectedProject === project.id ? "bg-indigo-600 text-white" : "text-indigo-300 hover:bg-indigo-900/60 hover:text-white"}`}><i className={`fa-solid ${icon} w-3 ${resource === "leads" ? "text-emerald-300" : ""}`} />{label}</Link>)}</div></details>)}{!projects.length && <p className="px-3 py-3 text-xs text-indigo-400">ยังไม่มีโครงการในระบบ</p>}</div></details></nav></div><div className="border-t border-indigo-950/40 bg-[#110e3b]/65 p-4"><p className="mb-3 px-1 text-xs text-indigo-300">MIDA Data Management</p><LogoutButton /></div></aside>;
}
